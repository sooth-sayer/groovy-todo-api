version: '2'

services:
  api:
    build: ./api
    ports:
      - "8080:8080"
    volumes:
      - ./api:/app
    depends_on:
      - db

  db:
    build: ./db
    ports:
      - "5432:5432"
    env_file:
      - db.env
